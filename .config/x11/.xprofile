#!/bin/sh

# Run upon x start

xinput set-prop DELL0922:00\ 04F3:30E3\ Touchpad libinput\ Tapping\ Enabled 1 & # Enable touchpad tap
feh --bg-fill ${HOME}/pictures/wallpapers/catInSnow.jpg & # Set background
git-auto -d "${HOME}/documents/orgs/notes" -i 600 -s origin -b master -p &
fcitx5 -d &
# nm-applet &
slstatus &
xautolock -time 10 -locker slock &
# picom &
udiskie --tray &

# start gnome-keyring service
eval $(gnome-keyring-daemon --start)
export SSH_AUTH_SOCK
source /etc/X11/xinit/xinitrc.d/50-systemd-user.sh
